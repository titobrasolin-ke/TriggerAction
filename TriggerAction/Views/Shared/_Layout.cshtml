<!DOCTYPE html>
<html lang="en-us">
<head>
    <title>Applicazione pilota Trigger Action</title>
    <style type="text/css">
        body, h1, h2, input, button {
            margin: 0;
            padding: 0;
            font: 16px 'Helvetica Neue', Helvetica, Arial, sans-serif;
        }

        .navbar {
            padding: 0 15px;
            color: #fff;
            position: relative;
            min-height: 50px;
            line-height: 50px;
            margin-bottom: 20px;
            border: 1px solid transparent;
            background-color: #003a6f;
            border-color: #080808;
        }

        h2 {
            padding: 0 0 10px 0;
            font: 36px 'Helvetica Neue', Helvetica, Arial, sans-serif;
        }

        input {
            padding: 10px;
            width: 50%;
        }

        button {
            padding: 10px;
        }

        #content {
            margin: auto;
            padding: 30px;
        }
    </style>
</head>
<body>
    <h1 class="navbar">Smart City Platform PROJECT</h1>
    <div id="content">
        @RenderBody()
    </div>
</body>
</html>
